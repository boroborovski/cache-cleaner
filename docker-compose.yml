services:
  cache-cleaner:
    build: .
    ports:
      - "5001:5001"
    volumes:
      - app-db:/data
      - ./ssh:/root/.ssh:ro
    environment:
      - ADMIN_PIN=changeme   # set to a strong PIN; leave empty to disable
    restart: unless-stopped

volumes:
  app-db:
